version: "3.9"
services:
  minima:
    image: minimaglobal/minima
    environment:
      minima_mdspassword: "123"
      minima_server: "true"
    ports:
      - 9001-9004
    volumes:
      - /home/minima/data
    restart: unless-stopped
